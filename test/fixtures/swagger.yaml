swagger: '2.0'

basePath: /api

info:
  version: '0.0.0'
  title: Sylo Edge API

consumes:
  - application/json

produces:
  - application/json

paths:
  /health:
    get:
      operationId: checkHealth
      description: |
        Health check
      responses:
        200:
          description: Successful response
